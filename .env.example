# Copy to .env and fill in. See the README for where each value comes from.
# postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<database>"

# Port the API listens on.
PORT=<port>
