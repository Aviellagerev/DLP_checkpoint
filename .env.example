# Matches the credentials in docker-compose.yml — copy this file to .env and it works as-is.
# Port 5433 is deliberate: the container publishes on 5433 to avoid colliding with a
# natively installed PostgreSQL, which usually occupies 5432.
DATABASE_URL="postgresql://dlp:dlp@localhost:5433/dlp"
PORT=3000
