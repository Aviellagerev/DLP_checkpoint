services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: dlp
      POSTGRES_PASSWORD: dlp
      POSTGRES_DB: dlp
    ports:
      - "5432:5432"
    volumes:
      - dlp_postgres_data:/var/lib/postgresql/data

volumes:
  dlp_postgres_data:
